[hub]
# Koji Hub Configuration

# Database configuration
DBName = koji
DBUser = koji
DBPassword = koji_password
DBHost = postgres
DBPort = 5432

# Server configuration
ServerName = koji-hub.koji.box
ServerAlias = koji-hub
ServerAdmin = admin@koji.box
DocumentRoot = /var/www/html

# SSL configuration
SSLCertificateFile = /etc/koji-hub/koji-ca-crt.crt
SSLCertificateKeyFile = /etc/koji-hub/koji-ca-crt.crt

# Koji configuration
KojiDir = /mnt/koji
KojiUser = koji
KojiGroup = koji

# Build configuration
BuildTimeout = 3600
MaxBuilds = 10

# Storage configuration
StoragePath = /mnt/koji
StorageURL = http://koji.box/downloads

# Logging
LogLevel = INFO
LogFile = /var/log/koji-hub/hub.log

# Security
AllowedHosts = localhost,koji-hub,koji-web,koji-worker
AllowedIPs = 172.20.0.0/16,127.0.0.1

# Authentication
AuthType = gssapi
LoginCreatesUser = On
KerberosServicePrincipal = hub/koji-hub.koji.box@KOJI.BOX
KerberosKeytab = /mnt/keytabs/hub.keytab

# Build system
BuildRootTimeout = 3600
MaxBuildsPerHost = 2


# The end.
